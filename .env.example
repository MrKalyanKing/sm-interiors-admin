# Base URL of the SM Interiors API, including the /api prefix.
VITE_API_URL=http://localhost:4000/api

# Public website URL — the sidebar links to it so the studio can check a change.
VITE_SITE_URL=http://localhost:5173
